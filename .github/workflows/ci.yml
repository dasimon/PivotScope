name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: preview

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: src/PivotScope.Web/package-lock.json

      # The EmbedSpa target requires a dist/: build the SPA before the C#.
      - name: Install SPA dependencies
        run: npm ci
        working-directory: src/PivotScope.Web

      - name: Build SPA
        run: npm run build
        working-directory: src/PivotScope.Web

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build -c Release --no-restore -p:SkipSpaBuild=true

      - name: Test
        run: dotnet test -c Release --no-build --filter "Category!=Integration"

      # CubeScope reminder: ADOMD pulls in a vulnerable version of
      # Microsoft.Identity.Client; the direct pin must stay effective after every upgrade.
      - name: Vulnerability audit
        run: dotnet list package --vulnerable --include-transitive 2>&1 | Tee-Object -Variable audit; if ($audit -match 'has the following vulnerable packages') { exit 1 }
        shell: pwsh

      - name: NPM audit
        run: npm audit --audit-level=high
        working-directory: src/PivotScope.Web
