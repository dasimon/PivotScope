<Project>

  <!-- Deliberate stop: MSBuild walks up the directory tree to the FIRST
       Directory.Build.props it finds. This empty file keeps PivotScope's
       conventions from applying to the CubeScope submodule, which has its own. -->

</Project>
