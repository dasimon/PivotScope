<Project>

  <!-- The CubeScope submodule puts its versions directly on its
       PackageReference items. PivotScope's central management would raise NU1008
       on each of them: disable it for everything under external/. -->
  <PropertyGroup>
    <ManagePackageVersionsCentrally>false</ManagePackageVersionsCentrally>
  </PropertyGroup>

</Project>
