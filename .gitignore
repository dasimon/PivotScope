bin/
obj/
.vs/
*.user
*.suo
node_modules/
dist/
publish/
artifacts/
TestResults/
*.xll

# Internal document regenerated by local tooling: it reintroduces company
# context on every run. Must not be published.
docs/PROJET.md

# Personal tooling, outside the project's scope
.clinerules

# Internal design specs and implementation plans — kept local, not published
docs/superpowers/
